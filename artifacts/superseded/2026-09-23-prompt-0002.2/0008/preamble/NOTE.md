Superseded 2026-09-23. Real runs of `0008 ## Preamble` under `prompt_version` `0002.2`
(no `label` field on a reference endpoint yet), from the original 21 call AC-14
coverage run. Replaced in `artifacts/runs/0008/preamble/` by a `0002.3` run made for
[experiment 0004](../../../../../experiments/0004-label-round-trip/README.md), the AC-7
`label` round trip check. Kept here, tracked in git, per `experiments/README.md`: never
delete a run's artifacts, move them.
