# Verify: model client router · spec 0012 · updated 2026-09-06

_Steps derived from spec 0012 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## UI / manual

None. This feature has no UI surface; every acceptance criterion is proved through code and the database.

## Commands

- [x] `pnpm typecheck` → passes → all ACs
- [x] `pnpm lint` → passes → all ACs
- [x] `pnpm test` → all pass, including `src/lib/ai/tiers.test.ts` and `src/lib/ai/client.test.ts` → AC-1, AC-3, AC-5, AC-6
- [x] `pnpm test:integration` → all pass, `test/integration/model-client-router.test.ts` and `test/integration-serial/model-client-router-usage-cap.test.ts` included, the two live vendor tests in `test/integration/model-client-router-live.test.ts` skipped → AC-4, AC-8
- [x] `TEST_LIVE_MODEL_CALLS_ENABLED=true pnpm test:integration -t "real vendor"` with real `OPENAI_API_KEY` / `GOOGLE_GENERATIVE_AI_API_KEY` → both tiers return a value matching the caller's schema → AC-1, AC-2. **Run by the engineer on 2026-09-06**, real keys passed as shell environment (not written into `.env.test`, which still holds placeholders): both `ai_scoring (OpenAI)` (3060ms) and `ai_check (Google)` (9782ms) passed against the real vendors.
- [x] `select call_type, scope, period, cap_value from public.usage_cap where call_type in ('ai_scoring','ai_check')` against the local stack → six rows at `500` / `1320` / `40000` → AC-8
- [x] `grep -rn "from \"@ai-sdk/" src --include=*.ts --include=*.tsx | grep -v '.test.ts' | grep -v 'src/lib/ai/tiers.ts' | grep -v 'src/lib/ai/client.ts'` → no output → AC-3. **Widened 2026-09-06 by `/check review`**: the guard itself (`src/lib/ai/tiers.test.ts`) now also bans the plain `ai` package outside `tiers.ts` and `client.ts`, and catches side-effect, dynamic and `require` import forms this hand-run grep does not; run `pnpm vitest run --project unit src/lib/ai/tiers.test.ts` for the fuller check.

## Acceptance-criteria coverage

- AC-1 (two tiers, two vendors, checked by reading the map) — met. `src/lib/ai/tiers.test.ts`, "maps ai_scoring and ai_check to two different vendors", re-run 2026-09-06 and passing; the live vendor command was also run by the engineer on 2026-09-06 against real OpenAI and Google endpoints and passed (see Commands), so the two tiers are now confirmed to hit two different vendors both statically and for real.
- AC-2 (`callTier()` returns the schema's parsed type inside a `Result`) — met. Confirmed three times: during `/check verify` (2026-09-06), a throwaway integration test proved the allowed-and-successful path against the real gate, then was deleted; the engineer separately ran the real, unmocked live vendor test and both tiers returned a value matching `ANSWER_SCHEMA`; `/test` (2026-09-06) then made the mocked path permanent in `src/lib/ai/client.test.ts`, "callTier(): the allowed path", with `withUsageGate` mocked at the module boundary so it runs with no database and no vendor call. This is now the test that catches a day to day regression here; the live vendor test stays the periodic real world check.
- AC-3 (no caller supplies vendor, model, or generation parameter) — met. `src/lib/ai/tiers.test.ts`, "imports no @ai-sdk/ or ai package outside tiers.ts and client.ts", widened 2026-09-06 by `/check review` after the original guard let a plain `import { generateObject } from "ai"` through; a parallel ESLint `no-restricted-imports` rule (`eslint.config.mjs`) now catches the same mistake at author time. The grep command above run by hand, no output.
- AC-4 (`checkUsageGate()` runs before dispatch; a refusal is a decided outcome, never a `Failure`) — met. `test/integration/model-client-router.test.ts` (the no-session case) and `test/integration-serial/model-client-router-usage-cap.test.ts` (the zeroed-cap case, moved there 2026-09-06 by `/check review` so it can never race `model-client-router-live.test.ts`'s real `ai_scoring` call), both re-run against the real local stack; the same-day throwaway test (see AC-2) additionally confirmed the allowed path reaches the vendor.
- AC-5 (a provider throw/timeout becomes `external_service_failed`) — met. `classify()`'s own pure logic: `src/lib/ai/client.test.ts`, "reads any other thrown value as external_service_failed"; the full wiring through `callTier()`, including that the `Failure`'s message is `AI_ROUTER_FAILURES`'s fixed text and never the vendor's own raw error: "callTier(): the failure path", "returns external_service_failed with the fixed message for any other thrown error" (`/test`, 2026-09-06).
- AC-6 (a schema mismatch becomes `response_malformed`) — met. `classify()`'s own pure logic: `src/lib/ai/client.test.ts`, "reads a NoObjectGeneratedError as response_malformed"; the full wiring through `callTier()`: "callTier(): the failure path", "returns response_malformed with the fixed message for a schema mismatch" (`/test`, 2026-09-06). **A truncated response (`finishReason: "length"`) is the same `NoObjectGeneratedError` class and stays classified as `response_malformed` on purpose** (fixed 2026-09-06, `/check review` minor 1): the router does not carry a `FailureKind` for "the output ceiling was too low", since adding one is a spec level decision this fix does not make. What it does add is a queryable `truncated: true` span attribute on `ai.call_tier`, so a run of `ai_scoring` hitting its `maxOutputTokens` ceiling (2048, a placeholder this spec's own rationale already flags as one feature 14 may have to raise) is visibly a truncation rather than reading as an ordinary vendor schema failure. Covered by two tests in `src/lib/ai/client.test.ts`: "marks a truncated response with a span attribute, but still classifies it as response_malformed" and "does not mark an ordinary schema mismatch as truncated".
- AC-7 (the named span opens first) — met. Confirmed 2026-09-06 by direct read of `src/lib/ai/client.ts`: `return Sentry.startSpan(...)` is the function's only statement, nothing precedes it; `docs/observability/spans.md`'s `ai.call_tier` row matches. Not independently re-proved by a forced-outage test in this feature's own suite (matching how `search.run`'s ordering is proved by code review plus the registry rather than a dedicated span-ordering test).
- AC-8 (six seeded `usage_cap` rows) — met. The `select` command above, re-run 2026-09-06 against the local stack: six rows at the correct values; also exercised indirectly by `test/integration-serial/model-client-router-usage-cap.test.ts`'s zeroed-cap test, which reads and restores the real rows rather than retyping their values.
- AC-9 (`DATA_RECIPIENTS` gains `openai` and `google-ai`; the privacy watchlist drops OpenAI, keeps Anthropic) — met. `src/features/legal/recipients.test.ts`, "names the eight companies data reaches today"; `src/app/(marketing)/privacy/page.test.ts`, "names no company the registry does not hold"; both re-run 2026-09-06.
- AC-10 (`OPENAI_API_KEY` / `GOOGLE_GENERATIVE_AI_API_KEY` required in `src/env.ts`, each provider built with the explicit key) — met. **Enforcement proved, not assumed, on 2026-09-06**: importing `src/env.ts` with `OPENAI_API_KEY` blank throws `Invalid environment variables` naming that exact key; `pnpm build` succeeds with real values present. `src/lib/ai/tiers.ts` passes `env.OPENAI_API_KEY` / `env.GOOGLE_GENERATIVE_AI_API_KEY` explicitly to `createOpenAI`/`createGoogle`, never left to the package's implicit `process.env` read.
