# Verify: entry page & link metadata · spec 0006 · updated 2026-08-28

_Steps derived from spec 0006 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

**Scope of this file.** The whole page is built. The first block of steps below covers the thin thread (build plan steps 1 to 3: the logo, the link metadata and the generated preview card); the block headed `## UI / manual: the five body sections` covers steps 4 to 11, appended 2026-08-28 when those landed.

## UI / manual

> **A protected preview URL cannot be used for the unfurl checks below.** Verified on 2026-08-28: this project has Vercel deployment protection on, so a preview URL answers an unauthenticated request with `302` to `vercel.com/sso-api`. A Slack or iMessage unfurler is exactly such a request, so it sees no `og:` tags and renders no card, for a reason that has nothing to do with this code. Do the unfurl checks one of these ways instead: on the **production** URL after merge (verified reachable and unprotected, `200`), or on a preview with protection bypassed for that URL. Do not read "no card appeared" on a raw protected preview as a failure of AC-11.

- [x] On an unprotected URL (production after merge, or a bypassed preview), paste it into Slack **and** into iMessage → both render a 1200 by 630 card showing the JobHunt lockup, the headline "Shows its work, not just a score." and the amber `8 / 11` chip → AC-11 · **confirmed by the engineer 2026-08-28**, against the pull request's preview through a shareable link, which is the protection bypass the note above calls for. The card renders in both clients
- [x] In that same paste → the card appears at all, despite `robots: index false` in `layout.tsx`. Spec 0006 records this as inferred rather than verified, so this step is what settles it → AC-12 · **confirmed by the engineer 2026-08-28. The inference was right:** a social unfurler is not a search crawler, and `noindex` does not suppress the card in either Slack or iMessage. This is no longer an assumption anywhere in the spec
> **`og:image` is expected to point at whatever host built it, including a preview host. That is correct, not a bug.** Next.js overrides `metadataBase` for a static metadata route file, which `opengraph-image.tsx` is, whatever `metadataBase` is set to. Verified in the installed source at `node_modules/next/dist/lib/metadata/resolvers/resolve-opengraph.js`, the `isRelativeUrl && (!metadataBase || isStaticMetadataRouteFile)` branch, whose comment states the intent: the image should be "properly discovered across different environments" without per environment `process.env` checks. It has to work this way, because production does not serve this route at all until the branch merges. **Do not "fix" `opengraph-image.tsx` or `src/lib/origin.ts` over this.**

- [x] View source on a **preview** deployment, in a browser where you are signed in to Vercel so protection lets you through → `og:image` is a fully qualified absolute URL ending `/opengraph-image`, on **that deployment's own host**. What is being checked is that the route resolves to a real absolute URL on the host serving it, not that it names any particular origin → AC-11 · confirmed 2026-08-28 against the signed in preview
- [x] Open that `og:image` URL directly in the same signed in browser → it returns the 1200 by 630 PNG, showing the JobHunt lockup, the headline and the amber `8 / 11` chip. This is the check that the route actually produces an image, independent of any unfurler → AC-11 · confirmed 2026-08-28 against the signed in preview, the image opened directly and shows the lockup, the headline and the 8 / 11 chip
- [ ] Repeat both after merge, on production → the same, resolved to the production host → AC-11 · **not run:** nothing to repeat on production until this branch merges. Blocked alongside the two unfurl steps
- [x] Same source view → `<title>` is `JobHunt`, the description is the one in the spec's `## Copy`, and `twitter:card` is `summary_large_image` → AC-10 · confirmed 2026-08-28 against the signed in preview: `<title>`, the description and `twitter:card` all match
- [x] Load the page at 320 pixels wide → the header shows the lockup and the Sign in control and no nav anchors; nothing overflows horizontally → AC-4, AC-14 · confirmed 2026-08-28 against the signed in preview
- [x] At 320 pixels, activate Sign in → the page jumps to the sign in band. It is a real link and must not 404 → AC-4, AC-7 · **now fully confirmed 2026-08-28**, once build step 9 created the band. Measured against the production build at 320 pixels: the URL changes to `#start`, the page scrolls to 4737 pixels, and the band lands 111 pixels down the viewport, clear of the 64 pixel sticky header
- [x] At 1024 pixels or wider → the three nav anchors (How it works, The reasoning, About) are visible and each jumps to its section → AC-4 · confirmed 2026-08-28 against the signed in preview
- [x] Tab from the top of the page → the logo link, then each nav anchor, then Sign in, each showing a visible teal focus ring that appears instantly rather than fading in → AC-14 · confirmed 2026-08-28 against the signed in preview: the whole tab order, logo link then each nav anchor then Sign in, each with a visible ring
- [x] With a screen reader → the header logo announces once, as the link's name ("JobHunt home"), not twice; the footer logo announces as "JobHunt" → AC-14 · confirmed 2026-08-28 against the signed in preview, VoiceOver announced the header logo once as "JobHunt home" and the footer logo as "JobHunt"
- [x] Footer → the lockup on the left and `© Ghaly Nicolas Jules` on the right, with nothing in the centre slot (reserved for feature 21) → AC-13 · confirmed 2026-08-28 against the signed in preview
- [x] Look at the page at 1440 pixels → the header sits on a visibly different tone from the hero below it, with no hairline rule between them → AC-3 · confirmed 2026-08-28 against the signed in preview

## Commands

- [x] `pnpm build` → succeeds, and the route table lists `/opengraph-image` as `○ (Static)`, confirming the card is generated once at build time rather than per request → AC-11 · confirmed 2026-08-28 by `/check verify`: compiled clean, and the route table prints `○ /opengraph-image` under `○ (Static) prerendered as static content`
- [x] `mv assets/SpaceGrotesk-SemiBold.ttf /tmp && pnpm build` → the build FAILS with an error naming `assets/SpaceGrotesk-SemiBold.ttf`. It must not succeed: `next/og` bundles `Geist-Regular.ttf` and would otherwise ship an off brand card silently. Restore the file afterwards → AC-15 · confirmed 2026-08-28 by `/check verify`: the build FAILED as required, with `Error: The social preview image needs assets/SpaceGrotesk-SemiBold.ttf, which is missing`, naming the licence and the fix. Font restored, tree clean
- [x] `ls assets/` → the `.ttf` sits beside `SpaceGrotesk-OFL.txt`, and that file is the SIL Open Font License 1.1 → AC-15 · confirmed 2026-08-28: both files present, and the licence is SIL Open Font License Version 1.1 whose PERMISSION & CONDITIONS clause grants the right to "use, study, copy, merge, embed, modify, redistribute" the Font Software, which is the permission AC-15 rests on
- [x] Change `--accent-300` in `src/app/globals.css` only, then `pnpm test` → `og-tokens.test.ts` fails. Revert → AC-16 · confirmed 2026-08-28: moving it to `#fcd582` failed `og-tokens.test.ts > the preview image's colours > accent still matches --accent-300 in globals.css`, 1 failed of 196. Reverted
- [x] Change one rectangle in `docs/design/logo/mark.svg`, then `pnpm test` → `logo.test.ts` fails the drift guard. Revert → AC-1 · confirmed 2026-08-28: widening one rect from 11 to 12 failed `logo.test.ts > Logo > has not drifted from docs/design/logo/mark.svg`, 1 failed of 196. Reverted
- [x] `grep -rn "use client" src/app src/features src/components` → no match anywhere in the entry page's tree → AC-4 · confirmed 2026-08-28, and the literal grep needs reading with care: it returns TWO hits, neither in this route's tree. `global-error.tsx` is Next's required root error boundary, and `dev-session/sign-in-form.tsx` belongs to `/sign-in` (feature 1's development only form). Proved properly by walking the import graph from `page.tsx` and `layout.tsx`: 24 files are reachable from `/` and NONE carries the directive
- [x] `grep -n "metadataBase" src/app/layout.tsx` → it reads `canonicalSiteUrl` from `src/lib/origin.ts`, never `currentOrigin()` and never a literal → AC-10. This is a source read rather than a browser check on purpose: for the generated image route Next overrides `metadataBase` (see the note above), so the rendered `og:image` cannot evidence this wiring either way. What `metadataBase` does govern is every other absolute URL the app will emit, such as the canonical links and `og:url` that feature 21's Terms and Privacy pages will need, so it still has to be right · confirmed 2026-08-28: line 56 reads `metadataBase: new URL(canonicalSiteUrl)`, and the file contains no literal origin and no `currentOrigin()`
- [x] `pnpm lint` → clean at `--max-warnings=0`, which is what enforces the no hand composed container rule → AC-1 · confirmed 2026-08-28, exit 0
- [x] `pnpm test` → all unit tests pass → AC-1, AC-16 · confirmed 2026-08-28: 196 passing across 19 files

## Acceptance-criteria coverage

Covered by the steps above: AC-1 (partly, lint plus the logo drift guard) · AC-3 (partly, the header boundary only) · AC-4 · AC-7 (partly, the header's jump link only) · AC-10 · AC-11 · AC-12 · AC-13 · AC-14 (partly, header and footer only) · AC-15 · AC-16.

**Manual pass, 2026-08-28.** Nine of thirteen manual steps confirmed against the signed in preview. **Every step that can be run at this point has been run, and all of them passed.** The four that remain are blocked rather than outstanding: three need either a deployment protection bypass or the branch merged to production, and one needs build step 9 to create the sign in band there is currently nothing to jump to. The reason sits on each line rather than being summarised away, so a later reader can tell a step blocked by circumstance from one that was ducked.

**A note on how AC-10 is proved.** Its two halves are checked in different ways on purpose. The title, description and Twitter card are read off the rendered page. The `metadataBase` half is proved by reading `layout.tsx`, because Next overrides `metadataBase` for the generated image route, so no amount of looking at `og:image` can tell a correct wiring from an incorrect one. An earlier version of this file asked for exactly that impossible check.

**`/check verify` run 2026-08-28: PASS, 17 of 17 acceptance criteria met.** Driven against the production build (`pnpm build && pnpm start`) in a real browser, not read off the code. Every criterion carries its own evidence on its step above. What this run added beyond the build time pass: the nine command steps, including all three that must FAIL on purpose (the font guard fails the build by name, and both drift guards fail their test when a token or a rectangle moves), plus an import graph walk proving no client component is reachable from `/`, and an independent re-check of the reduced motion fix.

**Three steps remain blocked, and none of them is an acceptance criterion.** The two unfurl checks and the production repeat need either a deployment protection bypass or the branch merged, and they settle spec 0006's inferred claims (that a social unfurler is not stopped by `robots: index false`, and that Twitter falls back to `og:image`). AC-11 and AC-12 are met by their own wording, which asks for a 1200 by 630 PNG from the route and the `robots` value in `layout.tsx`; both were confirmed directly. The residual risk is real but it sits in the spec's `## Follow-up`, not in the contract.

**Everything is now covered by a step.** The criteria this file once listed as unbuilt (AC-2, AC-3 in full, AC-5, AC-6, AC-7 in full, AC-8, AC-9, AC-17) have steps in the section below, added 2026-08-28. The only steps still blocked are the two unfurl checks and the production repeat, all three waiting on a merge or a protection bypass rather than on any code.

## UI / manual: the five body sections

_Added 2026-08-28 when build plan steps 4 to 11 landed. Every step here was run once against the production build (`pnpm build && pnpm start`) in a real browser at 1440 and 320 pixels, and the result is recorded on the line. They stay in the file because they are re-runnable: `/check verify` re-runs them against a deployment, and `/test` locks the ones that need no browser._

- [x] Measure `padding-top` on each of the five `<section>` elements at 1440 pixels → hero 160, how it works 64, the reasoning 160, about 96, sign in 96. Three distinct tiers, in the order generous, compact, generous, standard, standard → AC-2 · confirmed 2026-08-28, computed style
- [x] Repeat at 320 pixels → 128, 48, 128, 80, 80. The tiers stay distinct below the `sm` bump, so the rhythm is not a desktop only effect → AC-2 · confirmed 2026-08-28, computed style
- [x] Measure `background-color` on the same five → paper, sunken, sunken, paper, then `rgb(25, 70, 70)`, which is `--primary-800` → AC-3 · confirmed 2026-08-28, computed style
- [x] Count sections whose computed `border-top-width` is `1px` → exactly one, the reasoning section, which is the only boundary where the background does not change. Two means the alternation was edited without reapplying spec 0005's adjacency rule → AC-3 · confirmed 2026-08-28, computed style
- [x] Count elements on the page with the card radius and a non `none` `box-shadow` → exactly one, the hero result card. The other three cards (both comparison cards and the status card) have no shadow and sit on `--paper` → AC-5 · confirmed 2026-08-28, computed style
- [x] Read `text-align` and the inner column's `max-width` on the sign in band, then on any other section → identical, `start` and 1152 pixels. The band is neither centred nor narrowed; its dark ground is its only axis → AC-6 · confirmed 2026-08-28, computed style
- [x] List every `<a>` on the page with its `href` → exactly five, all real destinations: the home link, three in page nav anchors, and Sign in to `#start`. No provider control and no apply control appears in that list → AC-7, AC-17 · confirmed 2026-08-28
- [x] Read the hero and the band → each shows two provider labels carrying a `soon` status chip, and the line `Sign in isn't live yet. Coming soon with Google and GitHub.` This holds locally, on preview and in production alike, because the page has no `/sign-in` link to break → AC-7 · confirmed 2026-08-28
- [x] Read the status card against the At a glance table in `docs/scope/scope.md`, both directions → nothing under `working` that the scope does not mark `done` (it reads "nothing yet · this page is the first of it", because no user facing feature has shipped), and nothing under `planned` without a scope row (`email digests` is gone; `a no sign in demo account` is feature 31). Deliberately a human read: a test asserting this would encode the same reading twice → AC-8
- [x] Look at the hero result card → the eyebrow `Example result` sits at the top of the card header, and the wrapping `<figure>` carries the same words as its `aria-label`. The `<figcaption>` holds the job role, not the example label → AC-9 · confirmed 2026-08-28, accessibility tree read
- [x] Look at the card footer → `Apply on the real posting` with the external link icon, rendered as text. It takes no focus and has no `href`, because an example listing has nothing to apply to → AC-17 · confirmed 2026-08-28
- [x] Tab from the top at 1440 pixels → five stops in document order (logo, How it works, The reasoning, About, Sign in), each with a `2px solid rgb(41, 115, 115)` ring at a 2 pixel offset, and no sixth stop anywhere in the body → AC-14 · confirmed 2026-08-28, computed style on each stop
- [x] Load at 320 pixels and compare every element's right edge against the viewport → nothing crosses it, and both `documentElement` and `body` report zero horizontal overflow → AC-14 · confirmed 2026-08-28
- [x] Emulate `prefers-reduced-motion: reduce` and read the match cells' opacity within the first frame after load → every cell already at opacity 1, and every computed `animation-delay` at `0s` → AC-14 · **failed on the first run 2026-08-28** (five of eight cells still invisible 120ms in, the last arriving at 350ms), because `globals.css` zeroed `animation-duration` but not `animation-delay` and `match-cell` fills backwards. Fixed the same day in spec 0005's stylesheet and re-run: all eight opaque immediately, three runs identical, with the stagger confirmed still intact when motion is not restricted
- [x] Emulate `forced-colors: active` → the hero card keeps a 1px system coloured border (its shadow is discarded) and the reasoning section keeps its hairline → AC-14 · confirmed 2026-08-28, computed style
- [x] Emulate `prefers-contrast: more` → `--muted` resolves to `#474a51`, so every muted line on the page darkens at once → AC-14 · confirmed 2026-08-28, computed style

### Value sourcing, one step per row

_Spec 0006's `## Value sourcing` table names where every displayed value comes from. These exercise the source rather than the shape, so a value drawn from the wrong place fails here even when it looks right._

- [x] Read the `MatchBar` accessible names on the page → the hero says 8 of 11, the reasoning comparison says 8 of 11, and step 02 says 6 of 8. The proportions come from the caller, per spec 0005 AC-7, and the hero's are derived from the skill lists beside them, so a chip added to the matched list moves the bar with it → AC-5 · confirmed 2026-08-28, accessibility tree read
- [x] Count the hero card's matched and missing chips → eight and three, which is the 8 of 11 the score badge and the bar both state. Three numbers, one source → AC-9 · confirmed 2026-08-28
- [x] Change one entry in the hero's `MISSING_SKILLS` array and reload → the score badge, the match bar, the missing chips, the gap notes and the summary line all move together, because every one of them derives from that list. Revert → AC-9 · **run 2026-08-28 and it found two things.** Removing `AWS` correctly moved the bar and the badge to 8 of 10, but the summary sentence still read a hand written "8 of 11" and the gap note for the removed skill stayed on the page, because the notes lived in a second array beside the skills. Both are fixed: the sentence interpolates the counts, and each skill now carries its own note, so the list is one fact rather than three that have to agree. Re-run after the fix: all five move together, and the restored state renders 8 of 11 everywhere
- [x] `grep -n "accent" src/features/entry-page/score-badge.tsx` → the amber accent appears only on the score, which is what `globals.css` reserves it for → AC-5 · confirmed 2026-08-28: one class literal, `bg-accent-300` on the score badge, and nothing else on the page reaches for the amber
- [x] Read the page's copy against `docs/design/JobHuntLanding.tsx` → unchanged except where AC-7 and AC-8 require it, plus the four engineer written strings `COPY-1` to `COPY-4` used verbatim → confirmed 2026-08-28. **The AC-7 required deviations, named so a later reader diffing the prototype does not read them as drift:** the sign in band's body was reworded from the prototype's "Sign in with Google or GitHub and run your first search" (`JobHuntLanding.tsx` line 380) to "Sign in with Google or GitHub **is coming soon**", because an imperative inviting an action that cannot happen is the same untruth AC-7 removes from the controls; the two provider controls became labels; and the "try the demo" control was dropped from the hero and the band, as the header had already dropped it. Verified in the prerendered HTML, not just the source: `.next/server/app/index.html` carries the "is coming soon" wording
- [x] `grep -rn "text-\[" src/features/entry-page/` → no arbitrary font size anywhere. The prototype's 22px and 44px score figures render at `text-h3` and `text-h2` instead, because spec 0005's scale is closed → AC-1 · confirmed 2026-08-28: no match, so every size on the page comes from the locked scale
